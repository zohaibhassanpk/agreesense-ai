/// SnackbarType: Different types of snackbars
enum SnackbarType { success, error, warning, info }

/// FieldType: Different types of input fields
enum TextFieldType { email, password, name, phone, text, number }

/// LogLevel: Different levels of logging
enum LogLevel { debug, info, warning, error, critical }

/// SlideDirection: Different directions for slide transitions
enum SlideDirection { left, right, up, down }

/// ApiCallStatus: Different statues/states for the api calling
enum ApiCallStatus { initial, loading, success, error }
