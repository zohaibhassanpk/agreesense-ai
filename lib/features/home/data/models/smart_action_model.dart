import '../../domain/entities/smart_action.dart';

class SmartActionModel extends SmartAction {
  const SmartActionModel({
    required super.title,
    required super.message,
    required super.highlight,
  });
}
