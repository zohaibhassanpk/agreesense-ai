import '../../domain/entities/device_banner.dart';

class DeviceBannerModel extends DeviceBanner {
  const DeviceBannerModel({
    required super.title,
    required super.subtitle,
    required super.icon,
  });
}
